version: "3.8"

services:
  app:
    build: .
    container_name: interview-general
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./interview.db:/app/interview.db
    restart: unless-stopped
