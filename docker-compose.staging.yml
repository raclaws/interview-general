version: "3.8"

services:
  app:
    build: .
    container_name: interview-staging
    ports:
      - "8001:8000"
    env_file:
      - .env.staging
    volumes:
      - ./interview_staging.db:/app/interview.db
    restart: unless-stopped
